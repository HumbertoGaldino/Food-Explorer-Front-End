import styled from "styled-components";

export const ContainerPaymentForm = styled.form`
  div {
    background-color: transparent;
  }

  .firstRow {
    margin-bottom: 3rem;
  }

  .secondRow {
    display: flex;
    gap: 2rem;
    margin-bottom: 3rem;
  }
`;
